#!/bin/bash

# Enable Jupyter widgets extension for classic notebook
jupyter nbextension enable --py widgetsnbextension --sys-prefix

# JupyterLab 3.0+ has widgets built-in, but ensure proper configuration
jupyter lab clean
jupyter lab build --minimize=False
